import 'package:flutter/material.dart';

class XDarkThemeColors {
  // Primary background color (very dark gray/near-black)
  static const Color primaryBackground = Color(0xFF000000); // Or Color(0xFF14171A); // Slightly lighter

  // Secondary background color (slightly lighter dark gray)
  static const Color secondaryBackground = Color(0xFF1C1F23);

  // Card background color (similar to secondary background)
  static const Color cardBackground = Color(0xFF1C1F23);

  // Primary text color (white or very light gray)
  static const Color primaryText = Colors.white;

  // Secondary text color (light gray)
  static const Color secondaryText = Color(0xFF8899A6);

  // Hint text color (similar to secondary text)
  static const Color hintText = Color(0xFF8899A6);

  // Divider color (very dark gray, subtle)
  static const Color divider = Color(0xFF2F3336);

  // Primary accent color (X blue)
  static const Color primaryAccent = Color(0xFF1DA1F2);

  // Accent color on hover/focus (slightly lighter blue)
  static const Color accentHover = Color(0xFF65BFF5);

  // Icon color (often white or light gray)
  static const Color iconColor = Colors.white;

  // Muted icon color (similar to secondary text)
  static const Color mutedIconColor = Color(0xFF8899A6);

  // Button background color (primary accent)
  static const Color buttonBackground = Color(0xFF1DA1F2);

  // Button text color (white)
  static const Color buttonText = Colors.white;

  // Error color (red)
  static const Color error = Color(0xFFF44336);

  // Success color (green)
  static const Color success = Color(0xFF4CAF50);

  // Warning color (yellow/amber)
  static const Color warning = Color(0xFFFFC107);
}